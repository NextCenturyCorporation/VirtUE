The server that provides a Directory Service for SAVIOR.

Note: It appears that even if the ports are exported, samba-tool
doesn't work for managing the service in the docker container. May
need to run in the AD container or in another docker container in the
same subnet.
