package com.ncc.savior.desktop.sidebar.prefs;

/**
 * List of prefences that are usable in the system.
 *
 */
public enum DesktopPreference {
	FAVORITES, LAST_VIEW, LAST_SORT, DEFAULT_APPS;
}
